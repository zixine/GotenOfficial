name: GKI KernelSU Hosted ubuntu Build

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Maximize build space
        uses: easimon/maximize-build-space@master
        with:
          root-reserve-mb: 5120
          swap-size-mb: 1024
          remove-dotnet: 'true'
          remove-android: 'true'
          remove-haskell: 'true'

      - name: Setup Workspace
        run: |
          echo "================================================"
          echo "Starting GKI Build - KernelSU-Next/SuSFS"
          echo "Using AOSP Prebuilt Clang Toolchain"
          echo "================================================"
          
          # Clean old environment and create new directory
          echo "Cleaning old directory and preparing environment..."
          rm -rf gki .repo
          mkdir -p gki
          echo "✓ Workspace created"

      - name: Install Dependencies
        run: |
          echo "Updating system and installing dependencies..."
          sudo apt-get update
          sudo apt-get install -y git curl repo build-essential bc bison flex libssl-dev \
            libelf-dev python3 python3-pip unzip zip rsync libncurses-dev lld ccache \
            cmake ninja-build
          echo "[✓] Dependencies Installed"

      - name: Setup Clang 20 Toolchain
        run: |
          echo "Setting up Clang 20 Toolchain from AOSP prebuilts..."
          cd gki
          CLANG_URL="https://android.googlesource.com/platform/prebuilts/clang/host/linux-x86/+archive/62cdcefa89e31af2d72c366e8b5ef8db84caea62/clang-r547379.tar.gz"
          CLANG_VERSION="clang-r547379"
          CLANG_DIR="prebuilts-master/clang/host/linux-x86/$CLANG_VERSION"
          
          # Create a directory for the toolchain
          mkdir -p "$CLANG_DIR"
          
          # Download and extract the Clang toolchain directly (without temporary files)
          echo "Downloading and extracting $CLANG_VERSION..."
          curl -L "$CLANG_URL" | tar -xz -C "$CLANG_DIR"
          
          echo "Toolchain $CLANG_VERSION has been prepared at $CLANG_DIR"
          
          # Add path to environment
          echo "CLANG_DIR=$GITHUB_WORKSPACE/gki/$CLANG_DIR" >> $GITHUB_ENV
          echo "GKI_DIR=gki" >> $GITHUB_ENV

      - name: Setup Git Identity
        run: |
          echo "Setup git identity"
          git config --global user.email "kingfinix98@gmail.com"
          git config --global user.name "goten"
          echo "✓ Git identity configured"
          
      - name: Init Kernel Source
        run: |
          echo "🌱 [3/9] Inisialisasi repo kernel Android 12 GKI 5.10"
          cd gki
          repo init --depth=1 -u https://android.googlesource.com/kernel/manifest -b common-android12-5.10
          echo "✅ Repo berhasil di-init"

      - name: Sync Kernel Source
        run: |
          echo "🔄 [4/9] Sinkronisasi source kernel..."
          cd gki
          repo --trace sync -c -j$(nproc --all) --no-tags --fail-fast
          echo "✅ Source kernel selesai disinkronkan"

      - name: Upstream Rama update
        run: |
          cd gki/common
          echo "[6/15] Upstream linux from Rama"
          git remote add rama https://github.com/ramabondanp/android_kernel_common-5.10.git
          git fetch -j12 rama
          git cherry-pick 2afc3cd 8f27b49 || echo "[!] Some commits may not apply cleanly"
          echo "[✓] Upstream completed"
          
      - name: Add KernelSU-Next
        run: |
          echo "🧩 [5/9] Add KernelSU-Next and SUSFS"
          cd gki/common
          curl -LSs https://raw.githubusercontent.com/KernelSU-Next/KernelSU-Next/staging/kernel/setup.sh | bash -s staging
          echo "✅ KernelSU-Next added"

          echo "Clonning susfs4ksu"
          git clone https://gitlab.com/simonpunk/susfs4ksu/ -b gki-android12-5.10 sus
          echo "✅ Clonned"
          echo "Patching susfs4ksu"
          susfs=sus/kernel_patches
          cp -r $susfs/fs $(pwd)
          cp -r $susfs/include $(pwd)
          cp -r $susfs/50_add_susfs_in_gki-android12-5.10.patch $(pwd)
          patch -p1 < 50_add_susfs_in_gki-android12-5.10.patch
          echo "✅ SUSFS patched"

      - name: Apply inline patch for module.c and drm_atomic_helper.c
        run: |
          cd gki
          echo "patching module.c (change version mismatch to return 1)"
          sed -i '/pr_warn.*disagrees about version of symbol.*/,+1 s/.*/return 1;/' common/kernel/module.c
          echo "removing drm_atomic_check valid_clones"
          sed -i '/^static int drm_atomic_check_valid_clones/,/^}/d' common/drivers/gpu/drm/drm_atomic_helper.c
          echo "Removing drm_atomic_check_valid_clones call..."
          sed -i '/ret = drm_atomic_check_valid_clones/,/return ret;/d' common/drivers/gpu/drm/drm_atomic_helper.c
       
      - name: Add Configuration Settings
        run: |
          echo "Changing to configuration directory: gki/common..."
          cd gki/common

          echo "Adding configuration settings to gki_defconfig..."
          DEFCONFIG="arch/arm64/configs/gki_defconfig"

          # Add KSU configuration settings
          echo "CONFIG_KSU=y" >> $DEFCONFIG
          echo "CONFIG_KPM=n" >> $DEFCONFIG

          echo "CONFIG_KSU_SUSFS_SUS_SU=n" >> $DEFCONFIG
          echo "CONFIG_KSU_MANUAL_HOOK=y" >> $DEFCONFIG
          echo "CONFIG_KSU_KPROBES_HOOK=n" >> $DEFCONFIG
          
          # Add SUSFS configuration settings
          echo "CONFIG_KSU_SUSFS=y" >> $DEFCONFIG
          echo "CONFIG_KSU_SUSFS_HAS_MAGIC_MOUNT=y" >> $DEFCONFIG
          echo "CONFIG_KSU_SUSFS_SUS_PATH=y" >> $DEFCONFIG
          echo "CONFIG_KSU_SUSFS_SUS_MOUNT=y" >> $DEFCONFIG
          echo "CONFIG_KSU_SUSFS_AUTO_ADD_SUS_KSU_DEFAULT_MOUNT=y" >> $DEFCONFIG
          echo "CONFIG_KSU_SUSFS_AUTO_ADD_SUS_BIND_MOUNT=y" >> $DEFCONFIG
          echo "CONFIG_KSU_SUSFS_SUS_KSTAT=y" >> $DEFCONFIG
          echo "CONFIG_KSU_SUSFS_SUS_OVERLAYFS=n" >> $DEFCONFIG
          echo "CONFIG_KSU_SUSFS_TRY_UMOUNT=y" >> $DEFCONFIG
          echo "CONFIG_KSU_SUSFS_AUTO_ADD_TRY_UMOUNT_FOR_BIND_MOUNT=y" >> $DEFCONFIG
          echo "CONFIG_KSU_SUSFS_SPOOF_UNAME=y" >> $DEFCONFIG
          echo "CONFIG_KSU_SUSFS_ENABLE_LOG=y" >> $DEFCONFIG
          echo "CONFIG_KSU_SUSFS_HIDE_KSU_SUSFS_SYMBOLS=y" >> $DEFCONFIG
          echo "CONFIG_KSU_SUSFS_SPOOF_CMDLINE_OR_BOOTCONFIG=y" >> $DEFCONFIG
          echo "CONFIG_KSU_SUSFS_OPEN_REDIRECT=y" >> $DEFCONFIG

          # Add additional tmpfs config setting
          echo "CONFIG_TMPFS_XATTR=y" >> $DEFCONFIG
          echo "CONFIG_TMPFS_POSIX_ACL=y" >> $DEFCONFIG

          # Add additional config setting
          echo "CONFIG_IP_NF_TARGET_TTL=y" >> $DEFCONFIG
          echo "CONFIG_IP6_NF_TARGET_HL=y" >> $DEFCONFIG
          echo "CONFIG_IP6_NF_MATCH_HL=y" >> $DEFCONFIG

          # Add BBR Config
          echo "CONFIG_TCP_CONG_ADVANCED=y" >> $DEFCONFIG 
          echo "CONFIG_TCP_CONG_BBR=y" >> $DEFCONFIG
          echo "CONFIG_NET_SCH_FQ=y" >> $DEFCONFIG
          echo "CONFIG_TCP_CONG_BIC=n" >> $DEFCONFIG
          echo "CONFIG_TCP_CONG_WESTWOOD=n" >> $DEFCONFIG
          echo "CONFIG_TCP_CONG_HTCP=n" >> $DEFCONFIG 
          echo "CONFIG_DEFAULT_BBR=y" >> $DEFCONFIG 

          echo "Patching Manual Hook"
          curl -s https://raw.githubusercontent.com/GotenAjje/kernel_patches/refs/heads/main/syscall_hook/min_scope_syscall_hooks_v1.4.patch | patch -p1

          echo "Manual Hook Patched"

      - name: Remove check_defconfig
        run: |
          echo "🧹 [8/9] delete check_defconfig"
          cd gki
          sed -i 's/check_defconfig//' ./common/build.config.gki
          
      - name: Fix Clang Path
        run: |
          cd gki
          echo "Memperbaiki path toolchain clang ke clang-r547379..."
          
          # Perbaiki path clang di file build.config.common
          sed -i "s/clang-r416183b/clang-r547379/g" common/build.config.common
          echo "✓ Clang path fixed"

      - name: Set Kernel Name and Branding
        run: |
          echo "🎨 Branding kernel as BoltX-KsuXSF"
          cd gki
          BRAND="-BoltX-KsuXSF"
          echo "echo \"$BRAND\"" > ./common/scripts/setlocalversion
          chmod +x ./common/scripts/setlocalversion

          DEFCONFIG2="./common/arch/arm64/configs/gki_defconfig"
          sed -i '/^CONFIG_LOCALVERSION=/d' "$DEFCONFIG2"
          echo "CONFIG_LOCALVERSION=\"$BRAND\"" >> "$DEFCONFIG2"
          echo "CONFIG_LOCALVERSION_AUTO=n" >> "$DEFCONFIG2"

      - name: Build Kernel
        run: |
          cd gki
          echo "Membangun kernel dengan optimasi performa"
          
          export CC="ccache clang"
          export USE_CCACHE=1
          export CCACHE_DIR="/tmp/ccache"
          export PATH="$CLANG_DIR/bin:$PATH"
          
          ccache -M 5G
          ccache -s
          
          LTO=thin BUILD_CONFIG=common/build.config.gki.aarch64 build/build.sh -j$(nproc)
          echo "✓ Pembangunan kernel selesai berhasil"

      - name: Find and Display Image Info
        run: |
          cd gki
          find out -type f -name Image -exec ls -lh {} \;
          echo ""
          echo "=============================================="
          echo "GKI GOTEN KernelSU-Next/SuSFS + Completed!"
          echo "=============================================="

      - name: Upload Image to Pixeldrain
        id: upload
        run: |
          IMAGE_PATH=$(find gki/out -type f -name Image | head -n 1)
          if [ -z "$IMAGE_PATH" ]; then
            echo "❌ Image not found"
            exit 1
          fi
          RESPONSE=$(curl -s -u :53bc8bf8-584d-444e-97e3-715a1b3526bf -F file=@"$IMAGE_PATH" https://pixeldrain.com/api/file)
          FILE_ID=$(echo "$RESPONSE" | grep -oP '"id":"\K[^"]+')
          echo "link=https://pixeldrain.com/u/$FILE_ID" >> $GITHUB_OUTPUT

      - name: Show Download Link
        run: |
          echo "🎉 Build Finished!"
          echo "🔗 Link Image: ${{ steps.upload.outputs.link }}"
